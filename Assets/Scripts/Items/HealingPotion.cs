using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HealingPotion : ItemCollider
{
    // Start is called before the first frame update
    void Start()
    {
        //Set stats
        recovery = 10;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
